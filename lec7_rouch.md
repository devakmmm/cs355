## **network layer**

end to end delivery
achieves this by joining together multiple data link layer hops

why do we need ip address if we have mac address? key thing netowk layers does is a collection of devices owned by a single layer network so ip addresses allow  us to represent devices that are a part of a network

